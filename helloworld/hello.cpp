#include <iostream>
//using namespace std;

int main() {
    //cout << "Hello World!" << endl;
    /*
    double a = 3.1;
    double b (3.9); // "constructor initialization" syntax
    double c (7.1); // "uniform initialization" syntax

    int i = a ; // narrowing cast
    int j = b ; // idem 
    double d = i ; // widdening cast 
    double a_div2_wrong = 1.0/2.0*a; // what is wrong here ?? 
    
    std::string s="test" ;

    double x ; // declare x w/o specifying a value
    double A = 3.2; // C++ is case sensitive


     std::cout << "a = " << a << std::endl;
    std::cout << "b = " << b << std::endl;
    std::cout << "c = " << c << std::endl;
    std::cout << "i = " << i << std::endl;
    std::cout << "j = " << j << std::endl;
    std::cout << "d = " << d << std::endl;
    std::cout << "a_div2_wrong = " << a_div2_wrong << std::endl;
    std::cout << 1/2 << std::endl;
    std::cout << "(int)a = " << (int)a << std::endl;
    std::cout << "s = " << s << std::endl;
    std::cout << "x = " << x << std::endl;
    std::cout << "A = " << A << std::endl; */ 

    /* int x = 100; 
    if (x==100)
    {
        std::cout << "x is 100" << std::endl;
    }
    else
    {
        std::cout << "x is not 100" << std::endl;
    }

    if (x>0)
        std::cout << "x is positive" ;
    else if (x < 0)
        std::cout << "x is negative" ; 
    else
        std::cout << "x is 0" ;
    std::cout << "\n" ;


    if ((x=10))
    {
        std::cout << "Entered\"if\"␣body␣\n"; 
    }
    std::cout << "x = " << x << "\n";

    return 0 ;*/

    // WHILE LOOP
    /* int n = 0 ; 
    while (n<3)
    {   //body of while loop
        std::cout << n << ", ";  // prints value of n 
        ++n ;                    // increase n by 1
    }
    std::cout << "while loop is over" << std::endl; 

    // DO-WHILE LOOP
    std::string str; // declare variable str of type string
    do { // begin do-while loop
        std::cout << "Enter \"bye\" to stop" << std::endl ;
        std::getline(std::cin, str); //store user input in str
    } while (str != "bye") ; // end do-while loop
    std::cout << "do-while loop is over" << std::endl ; */

    // RANGE-BASED "FOR" LOOPS
    /*std::cout << __cplusplus << std::endl;
    std::string str2 {"Hello!"} ; 
    for (char c : str2 )
    {
        std::cout << "[" << c << "]" ;
    }
    std::cout << '\n' ; 

    int array[2] {8,9} ;
    for (int i : array)
    {
        std::cout << "[" << i << "]" ;
    }
    std::cout << std::endl; */

    // POINTERS


    } // end main


